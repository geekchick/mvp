<h3>Edit Your Videos</h3>
<%= form_for (@user) do |f| %>
	<%= f.text_field :video_url %><%= f.submit "Update", :class => 'btn btn_profile' %>
	<% end %>

	<% if @user.video_url.nil? %>
	
	<% else %>
	<% aha = UnvlogIt.new(@user.video_url) %>
	<%= aha.embed_html(300, 266).html_safe %>
<% end %>
