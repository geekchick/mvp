	<%= form_for (@user) do |f| %>
		<%= f.text_field :video_url %><%= f.submit "Update" %>
	<% end %>

	<% if @user.video_url == "" %>
		redirect_to root_path_url
	<% else %>
		<% aha = UnvlogIt.new(@user.video_url) %>
		<%= aha.embed_html(300, 266).html_safe  %>
	<% end %>
